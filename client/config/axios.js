import axios from 'axios'
import {baseURL} from './constants'

// // Set the base url for calling API's
// axios.defaults.baseURL = baseURL + '/api'
// const token = localStorage.getItem('farmerAppJWT')
// if (token) addTokenToAxiosInstanceConfig(token)

// function addTokenToAxiosInstanceConfig(token) {
//   axios.defaults.headers.common['Authorization'] = token
// }

// export {addTokenToAxiosInstanceConfig}
